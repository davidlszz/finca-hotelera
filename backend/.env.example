# ── Local (SQLite, sin cambios) ───────────────────────────────────
PORT=3001
JWT_SECRET=finca_hotelera_usc_2026_super_secret_key_change_in_production
JWT_EXPIRES_IN=8h
NODE_ENV=development

# ── Producción (Vercel + Neon PostgreSQL) ────────────────────────
# DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/finca_hotelera?sslmode=require
# JWT_SECRET=<genera uno largo y aleatorio>
# FRONTEND_URL=https://finca-hotelera.vercel.app
